# Copy this file to .env and fill in real values
# Generate AES_KEY: python -c "import os; print(os.urandom(32).hex())"
# Generate JWT_SECRET: python -c "import os; print(os.urandom(32).hex())"

AES_KEY=replace_with_64_hex_characters
JWT_SECRET=replace_with_64_hex_characters
